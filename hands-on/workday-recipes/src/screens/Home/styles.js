import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    screenContainer: {
        marginHorizontal: 24,
    },
    searchView: {
        marginTop: 20,
    },
    featuredSection: {
        marginTop: 36,
    },
    categoryView: {
        marginTop: 36,
    },
    sectionTitle: {
        fontSize: 16,
        fontWeight: "600",
    },
    featuredList: {
        flexDirection: "row",
        gap: 16,
    },
});

export default styles;
