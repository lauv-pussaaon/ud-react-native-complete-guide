import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    listView: {
        marginHorizontal: -24,
    },
    recipeList: {
        flexDirection: "row",
        gap: 12,
    },
});

export default styles;
