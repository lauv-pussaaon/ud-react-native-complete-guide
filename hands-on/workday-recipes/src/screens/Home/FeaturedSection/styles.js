import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    listView: {
        marginHorizontal: -24,
    },
    featuredList: {
        flexDirection: "row",
        gap: 16,
    },
});

export default styles;
