import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 20,
        fontWeight: "700"
    },
    titleContainer: {
        marginVertical: 16,
    },
});

export default styles;