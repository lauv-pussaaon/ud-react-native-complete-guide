import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    backButton: {
        width: 36,
        height: 36,
    },
    buttonContainer: {
        position: "absolute",
        right: 10,
        top: 22,
        zIndex: 1,
        opacity: 0.85,
    },
});

export default styles;
