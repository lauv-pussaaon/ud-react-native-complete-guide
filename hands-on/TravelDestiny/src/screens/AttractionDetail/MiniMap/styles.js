import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    map: {
        marginTop: 32,
        width: "100%",
        height: 300,
    },
    fullMapLink: {
        marginVertical: 16,
        marginLeft: "auto",
        marginRight: "auto",
    },
    fullMapText: {
        color: "hsla(202 80% 46% / 1)",
        fontWeight: "500",
        fontSize: 16,
        textTransform: "uppercase",
    },
});

export default styles;
