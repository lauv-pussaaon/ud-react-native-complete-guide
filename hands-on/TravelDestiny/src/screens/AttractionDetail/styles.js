import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        marginHorizontal: 22,
        marginTop: 32,
        position: "relative",
    },
});

export default styles;
